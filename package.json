{
  "name": "galaxy-explorer-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Galaxy Explorer — a Star Wars catalog demo app for mabl browser-test automation",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "http-server": "^14.1.1",
    "vitest": "^2.1.9"
  }
}
